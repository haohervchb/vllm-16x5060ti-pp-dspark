# Reserve enough prefetchable MMIO below each PCIe hot-plug root port for
# eight 16 GiB GPU BAR1 apertures, and keep the bare-metal Intel IOMMU off.
GRUB_CMDLINE_LINUX_DEFAULT="${GRUB_CMDLINE_LINUX_DEFAULT} intel_iommu=off pci=realloc=on,hpmmioprefsize=512G"
